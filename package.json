{
  "name": "merit-aktiva",
  "version": "0.0.2",
  "description": "Typesafe wrapper for integrating with Merit Aktiva accounting software API",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "vitest run",
    "build": "tsup",
    "lint": "tsc",
    "ci": "pnpm run lint && pnpm run test && pnpm run build",
    "release": "pnpm run lint && pnpm run test && pnpm run build && changeset publish"
  },
  "repository": "https://github.com/KasparKipp/merit-aktiva",
  "keywords": [
    "360",
    "Księgowość",
    "Merit",
    "Aktiva"
  ],
  "author": "Kaspar Kipp",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@types/node": "^20.11.5",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.1"
  }
}
